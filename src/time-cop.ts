import { TimeLimitSet } from "../generated/TimeCop/TimeCop"
import { FreeNFTDrop } from "../generated/schema"

export function handleTimeLimitSet(event: TimeLimitSet): void {
  // let transaction = event.receipt?.logs.filter(log => );

  // let drop = new FreeNFTDrop(event.params.collection)
  // drop.deadline = event.params.deadline
  // drop.save()
}
